<h1>Portfolio Items</h1>

<h2><% @portfolio_item.each do |p| %>
      <p><%= p.title %></p>
      <p><%= p.subtitle %></p>
      <p><%= p.body %></p>
      <%= image_tag p.thumb_image if !p.thumb_image.nil? %>
    <% end %>
</h2>